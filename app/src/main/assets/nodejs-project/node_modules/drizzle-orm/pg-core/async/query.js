import { PgRelationalQuery } from "../query-builders/query.js";
import { entityKind } from "../../entity.js";
import { tracer } from "../../tracing.js";
import { applyMixins } from "../../utils.js";
import { QueryPromise } from "../../query-promise.js";

//#region src/pg-core/async/query.ts
var PgAsyncRelationalQuery = class extends PgRelationalQuery {
	static [entityKind] = "PgAsyncRelationalQueryV2";
	/** @internal */
	_prepare(name, generateName = false) {
		return tracer.startActiveSpan("drizzle.prepareQuery", () => {
			const { dialect } = this;
			const isFirst = this.mode === "first";
			const { query, builtQuery } = this._toSQL();
			const shape = dialect.shapeGenerator?.({
				type: "relational",
				fields: query.selection
			}, void 0);
			const mapper = shape ? isFirst ? (rows) => rows[0] : void 0 : dialect.mapperGenerators.relationalRows({
				isFirst,
				parseJson: this.parseJson,
				parseJsonIfString: false,
				rootJsonMappers: false,
				selection: query.selection,
				arrayModeRoot: true
			});
			return this.session.prepareQuery(builtQuery, shape ? "objects" : "arrays", name ?? generateName, mapper, void 0, void 0, shape);
		});
	}
	prepare(name) {
		return this._prepare(name, true);
	}
	execute(placeholderValues) {
		return tracer.startActiveSpan("drizzle.operation", () => {
			return this._prepare().execute(placeholderValues);
		});
	}
};
applyMixins(PgAsyncRelationalQuery, [QueryPromise]);

//#endregion
export { PgAsyncRelationalQuery };
//# sourceMappingURL=query.js.map