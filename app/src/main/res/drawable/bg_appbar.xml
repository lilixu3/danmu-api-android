<?xml version="1.0" encoding="utf-8"?>
<!-- AppBar subtle scrim (keeps content readable, feels "glass" without being heavy) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/appbar_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/appbar_stroke" />
</shape>
